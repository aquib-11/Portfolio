import styled from "styled-components";

const Wrapper = styled.div`
  display: grid;
  margin: 3rem;
  color: white;
  button {
    padding: 0.5rem;
    font-size: 1.1rem;
  }
  a {
    color: var(--color);
  }
`;
export default Wrapper;
